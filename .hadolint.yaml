ignored:
  # Pinning apt versions on Debian rolling repos provides little safety and
  # frequently breaks at the next snapshot. The base image is already pinned.
  - DL3008

trustedRegistries:
  - docker.io
